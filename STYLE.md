# 執筆スタイルガイド

本書の全章はこの規約に従う。レビューもこの規約を基準に行う。

## 文体

- 敬体(です・ます調)で統一する
- 地の文と箇条書きの各項目は、述語(です/ます/ください/でしょうか など)で終える。
  体言止めを使わない
  - 名詞句で終えてよい例外: 見出し、「この章でわかること」の列挙、表のセル、
    図のキャプション、mermaidのラベル、frontmatterの`description`、用語集の定義文
  - 「**用語**(english) — 説明」形式の箇条書きは書かない。
    「**用語**(english)は、〜です」と文にするか、
    「**用語**(english): 〜します」と半角コロンで区切って述語で終える。
    コロンの前のラベルは名詞句でよいが、項目の末尾は述語で終える
- 否定してから肯定する二文構成(「〇〇はXXではありません。△△です。」)を
  書かない。「〇〇は△△であり、XXではありません。」か「〇〇は△△です。」と書く。
  一文の中で主張を対比する場合も、肯定を先に置く(「△△であり、XXではない」)。
  単位や数値の事実の対比(「1バイト単位ではなくライン単位で」)は「ではなく」でよい
- 読者を「あなた」と呼ばない。必要な場合は「読者」と書くか、
  「〜してください」の形で指示する
- 一文は短くする。読点で文を連結しすぎない
- 実測値は「実測値は**873ミリ秒**で、〜」のように文の中に書く。
  「実測: **873ミリ秒**。」のような体言止めの記法は使わない
- 太字の手順ラベルも文にする(「**1. GPUに接続します。**」)。
  手順が長い場合はh3見出しに分ける
- 内容を直接書く。内容のない前置きの文を置かない
  - 禁止例: 「〜はこうです。」「〜はこれです。」「注意を1つ。」「補足を2つ。」
    「先に言葉の注意を1つ。」「種明かしをすると」「答え合わせ」
  - 項目数を示すときは「注意点は次の2つです。」のように述語のある文で書く
  - 「実は」「さて」「なんと」のような演出のための語を使わない
  - 「帰結」「含意」のような抽象語で因果をつながない。
    「このため」「〜から生じる」「〜によって決まる」と具体的に書く
- 誇張表現(「爆速」「劇的」「桁違い」など)を使わない。速度差は数値で示す
- 主観的な評価語(「面白い」「強力な」「美しい」など)で性質を説明しない。
  何がどうなるかを書く

## 記号

- ダッシュ(—、——)を使わない
  - 文中の区切りには句点「。」を使う
  - 補足には半角括弧を使う
  - 見出しの副題、箇条書きの「項目: 説明」の区切りには
    半角コロン+半角スペースを使う(例: `## 実験: CPUの速さを体感する`)
  - この規則はコード内コメント、mermaidのラベル、SVGのテキストにも適用する
- 括弧は半角`()`を使う。全角`（）`は使わない
- 引用符は「」を使う。比喩や俗語を「」で囲んで使うことは認めない
  (比喩そのものを書かない)
- 個数は「1つ」「2つ」と算用数字で書く(「一つ」「ひとつ」と混在させない)
- 強調の`**`は、括弧・鉤括弧・句読点・バッククォートを内側に含めない。
  CommonMarkの規則で、直前が文字で直後が記号の`**`は開き記号に、
  直前が記号で直後が文字の`**`は閉じ記号に認識されず、`**`が文字として残る
  - 誤: `**873ミリ秒(2.5 GFLOP/s)**です` / `せず**`v[7]`と表示**した`
  - 正: `**873ミリ秒**(2.5 GFLOP/s)です` / `せず、`v[7]`と表示して**正常終了**した`
  - ビルド後に `dist/` 内のHTMLへ`**`が文字として残っていないことを確認する

## 用語

- 専門用語は初出時に必ず説明する。初出は `<span class="term">用語</span>`
  ではなく **太字** + 英語併記で示す。例: **キャッシュライン**(cache line)
- 初出で説明した用語は付録の用語集(appendix/glossary.md)にも必ず収録する
- 用語集の定義文は常体で「〜。」で終える辞書形式とし、末尾に初出章へのリンクを置く
- 不要なカタカナ語・ジャーゴンを避ける。定着した訳語があれば訳語を使う
- 訳語が不自然な用語(キャッシュ、パイプラインなど)はカタカナのままでよい

## 比喩

- 比喩(暗喩・隠喩・慣用句)を使わない。物理的な動作、人の行為・感情、生物、
  日常の物体を表す語を、計算機やプログラムの動作の説明に転用しない
- 定着した専門用語・訳語はこの限りではない。
  例: 走査、分岐の飛び先、親プロセス/子プロセス、待ち行列、飢餓(starvation)、
  衝突(collision)、追い出し(eviction)、桁落ち、桁あふれ、書き戻し、先読み、
  レイテンシの隠蔽(latency hiding)、ホットループ、ホットスポット、ボトルネック、
  偽共有(false sharing)、壁時計時間(wall-clock time)、ウォームアップ、
  流れ作業(パイプラインの説明)
- 言い換えの例:

| 使わない表現 | 言い換え |
| --- | --- |
| 落ちる(プロセスが/遅い経路に/精度が) | 異常終了する / 低速な経路で処理される / 低下する |
| 閉じる(体系を) | 完成させる / 欠けている部分を補う |
| 立てる(スレッドを/仮説を) | 生成する / 設定する |
| 罠 | 誤りやすい点 / 注意点 |
| 主役 / 脇役 | 中心 / 主要な手段 / 補助的な手段 |
| 住所 | アドレス |
| 土台 / 柱 | 前提 / 基礎 / 主題 |
| 橋渡し | 接続 / 導入 |
| 種明かし / 答え合わせ | 仕組みの説明 / 検証 |
| 素通りする | 説明を省く |
| 歯抜け | 不連続 |
| こぼれる(誤差が) | 失われる |
| 刺さる(ペナルティが) / 踏む(分岐ミスを) | 発生する |
| 走る(コードが) | 実行される |
| 引っ越し(レジスタ間の) | 転送 |
| 沈む(値が) | 減少する |
| 地図 | 一覧 / 全体像 |
| 壁 | 上限 / 制約 |
| 勝つ / 負ける(手法が) | 上回る / 下回る |
| 効く(最適化が) | 適用される / 効果がある |
| 刻む(拍を) | 動作する(クロックが) |
| 手元にある(レジスタが) | CPU内部にある |
| 生きる(変数が) | 存在する / 保持される |
| 食う(メモリを) | 消費する |
| 化ける(値が) | 別の値になる / 破損する |
| 鎖(依存の) | 連鎖 |
| 閉じた式 | 閉形式の式 |
| 手の空いた(スレッド) | 処理を終えた / 待機状態の |
| 領分 | 担当 |
| 奪い合い / 取り合い | 競合 |
| 覆う(TLBが) | 対応できる範囲 |
| 溜める | 保持する |
| 畳む(配列を) | 集約する |
| 約束(レジスタの) | 取り決め / 規約 |
| 支払う(レイテンシを) | 発生する / 加算される |
| 載る(キャッシュに) | 格納される |
| 高くつく | コストが大きい |
| 熱い(フィールドが) | 頻繁に使う |
| 投げる(GPUに処理を) | 移す / 実行させる |
| 載せる(データをGPUに) | 配置する / コピーする |
| 使い回す | 再利用する / 繰り返し読む |
| 打ち手 / 手(対策の) | 対策 / 手法 |
| 骨格 / 流儀 / 儀式 | 構造 / 方法 / 定型の手順 |
| 心臓部 | 中心 |
| いよいよ / いきなり / ついに | (削除) |
| 仕事(GPUやスレッドの) | 処理 |
| 投げる(コマンドを) | 送信する / 発行する |
| 殺到する | 集中する |
| 処方箋 | 対策 |
| 〜の上に立つ | 〜を前提にする |
| ビットが立つ | ビットが1になる |
| 犯人(遅さの) | 原因 |
| 難所 | 難しい問題 |
| 世界(1スレッドの) | 範囲 / 環境 |
| 封じる | 防ぐ |
| 跳ねる(レイテンシが) | 増える |
| 道連れにする | 停止させる |
| 計画書 / 入れ物 | 値 / 構造体 |
| 署名する(unsafeに) | 宣言する |
| 作法 | 規則 |
| 値段 | コスト |
| 潮流 | 方向 / 傾向 |
| 焼き込む(ハードウェアに) | 組み込む |
| 冷える(キャッシュが) | 内容が失われる |
| 汚す(キャッシュを) | 内容を置き換える(「キャッシュ汚染」は定着語として可) |
| 実戦投入 | 実用 / 実際の利用 |
| 思想(設計の) | 設計方針 / 考え方 |
| 伸びしろ | 改善の余地 |
| 計器 | カウンタ / 計測手段 |
| 解像度を上げる | 詳しく説明する |
| 実体化する(ページが) | 物理メモリが割り当てられる |
| 恐ろしい / 劇的な(性質) | 影響が大きい |
| 黙って / 勝手に / 律儀に | 検査なしで / 常に(または削除) |

- 「手元のMac」「手元の環境」「手元で再計測」は、本書全体で
  ローカル実行環境を指す定型表現として使ってよい

- 擬人化を使わない。「CPUくんは困ってしまいます」のような表現に加え、
  装置に意図や性格を与える副詞(「律儀に」「黙って」「勝手に」「賢く」
  「素直に」「大胆に」)も使わない
  - 処理内容を指す定型の動詞(判断する、検出する、選択する、予測する)は使ってよい
- 直喩は「例えば、XXのようなものです」の形式のみ、説明に適する場合に限り
  使ってよい。直喩の直後に、対応関係を字義どおりの語で説明する

## 数値

- レイテンシ・容量などの数値は「おおよそ」「〜程度」と概数であることを明示する
- 実測値を載せる場合は計測環境を明記する
- 根拠のない断定的な倍率(「10倍速くなります」)を書かない

## コード

- ブラウザで実行可能なコードは `src/snippets/chNN/*.rs` に置き、
  `<RustPlay snippet="chNN/name" />` で埋め込む(表示・実行・コンパイル検証の
  単一ソース。ビルド時に scripts/preprocess-docs.mjs がコードを展開する)
- 実行させる意味のないコード断片は通常のコードフェンスで表示する
- スニペットはすべて `rustc --edition 2024` でコンパイルが通ること
- nightly 機能を使うスニペットは `src/snippets/chNN/nightly-*.rs` と命名し、
  `<RustPlay channel="nightly">` で埋め込む
- 計測系スニペットは Playground のタイムアウト(実行10秒程度)内に収まる規模にする
- `<RustPlay>` の `title` 属性も本文と同じ比喩・ダッシュの規則に従う
- コード内コメントは日本語で要点のみを書く。比喩とダッシュの規則は
  コード内コメントにも適用する(体言止めは可)

## 章の構成

1. 冒頭: 「この章でわかること」の箇条書き(3〜5項目)
2. 本文: 見出しは h2/h3 のみ。1つの h2 は1つの主題
3. 実験(実行可能コード)を最低1つ含める(GPU章はローカル実行手順で代替可)
4. 末尾: 「まとめ」(箇条書き) + 次章の主題を示す1〜2文

## 図

- 概念の流れ・構造は mermaid コードブロックで書く
- mermaid で表現が崩れる図(パイプライン段の時間重なりなど)は
  インラインSVG(`.book-figure` でラップ、figcaption 付き)で書く
- 図には必ず本文からの言及を置く(「図だけ置いて説明しない」を禁止)

## リンク

- 章間リンクは絶対パス(`/cpu/01-how-code-runs/` 形式)で書く
- 外部リンクは公式ドキュメント・一次情報を優先する

## 補足の取り決め

- 「この章でわかること」の箇条書きは章の予告であり、用語の正式な
  初出定義(太字+英語併記)は本文中で行う

## 応用編(ch13-28)の追加規約

- 基礎編の該当章への後方参照を必ず置く(体系の接続を明示する)
- 各章の「この章でわかること」の末尾に「なぜ体系に必須か」を1項目含める
- 実験はplayground優先。不可能なもの(GPU/OSツール等)はexamples/ + 手元実測
- 実験不能な主題は「観測手順の提示」で代替する
- 定石が実測で覆った場合は、隠さずそのまま載せて考察する
